# 优化更新日志

## 日期：2026-07-15

### 🐛 Bug修复

#### 1. 修复智慧树新形态课程"只跳转必学章节"功能失效 (#317)
**文件：** `packages/scripts/src/projects/zhs.ts`

**问题描述：**
- `getNextJob` 函数未参考 `switchMode` 配置
- 导致"只跳转必学章节"模式无法正确跳过"选学资源"

**解决方案：**
- 重构 `getNextJob` 函数，根据 `switchMode` 配置筛选卡片
- `job` 模式：只收集必学区域的卡片，跳过包含"选学"的区域
- `all` 模式：收集所有卡片
- 增加边界条件处理，当没有卡片时返回 `undefined`
- 修复 `start_index` 可能为 -1 的bug

**影响：** 智慧树新形态课程用户

---

#### 2. 修复ScriptCat环境下题库请求超时问题 (#281)
**文件：** 
- `packages/core/src/core/utils/request.ts`
- `packages/core/src/core/answer-wrapper/answer.wrapper.handler.ts`

**问题描述：**
- ScriptCat 基于 Service Worker，有30秒空闲自动挂起机制
- Service Worker 挂起后，`GM_xmlhttpRequest` 的回调永远不会触发
- 用户需等待完整的"搜题最大耗时"才能得到超时错误

**解决方案：**
1. `request` 函数增加 `timeout` 参数（默认60000ms）
2. `GM_xmlhttpRequest` 增加 `timeout` 和 `ontimeout` 回调
3. `fetch` 使用 `AbortController` 实现超时控制
4. `onerror` 传递 Error 实例而非原始对象，改善错误信息
5. 调用方传入实际的超时配置

**技术要点：**
- GM_xmlhttpRequest 的 timeout 参数在请求层面设置超时
- ontimeout 超时时立即 reject
- fetch 使用 AbortController 在超时时中断请求
- 错误信息从 `err?.error` 提取，更清晰

**影响：** 所有使用ScriptCat的用户，特别是配置了较长超时时间的用户

---

#### 3. 优化多题库不同答案时的处理逻辑 (#279)
**文件：** 
- `packages/core/src/core/worker/resolvers/multiple.ts`
- `packages/core/src/core/worker/resolvers/single.ts`

**问题描述：**
- 使用多个题库时，若返回不同答案，会走相似匹配流程
- 应该优先选择"出现次数最多"的答案

**解决方案：**

**多选题投票机制：**
- 对每个题库的答案组合进行投票统计
- 将选项排序后作为key，确保相同答案组合能被识别
- 选择出现次数最多的答案组合
- 如果次数相同，则选择总评分最高的
- 投票机制失败时，回退到原有的最高匹配度逻辑

**单选题投票机制：**
- 在匹配流程前增加投票阶段
- 统计所有答案（归一化后）的出现次数
- 如果某个答案出现次数超过1次，优先使用它进行匹配
- 投票失败时，继续原有的匹配流程

**技术要点：**
- 使用 Map 进行投票统计
- 多选题使用排序后的选项组合作为key
- 单选题对答案进行归一化处理
- 保持向后兼容，不影响单题库场景

**影响：** 所有配置了多个题库的用户

---

### 📝 代码质量改进

1. **改善错误处理**
   - request 函数的错误信息更加清晰
   - 区分超时错误和连接错误
   - 改善 HTTP 错误信息的展示

2. **增加边界条件处理**
   - getNextJob 函数处理空卡片列表的情况
   - 处理 start_index 为 -1 的情况

3. **增强代码注释**
   - 详细说明投票机制的实现逻辑
   - 补充超时处理的技术要点

---

### 🔧 配置优化

**搜题最大耗时调整：**
- 最大值从 60秒 增加到 180秒（3分钟）
- 默认值从 60秒 增加到 120秒
- 支持使用大模型等需要更长时间的题库

**相关Issue：** #260, #270

---

### 📊 测试建议

1. **智慧树新形态课程测试**
   - 测试"只跳转必学章节"模式
   - 验证选学资源是否被正确跳过
   - 测试外链模式是否正常

2. **ScriptCat超时测试**
   - 在ScriptCat环境下配置慢速题库
   - 验证超时是否在配置时间内触发
   - 检查错误信息是否清晰

3. **多题库答案投票测试**
   - 配置2-3个题库返回不同答案
   - 验证是否选择出现次数最多的答案
   - 测试单题库场景是否不受影响

---

### 🎯 后续计划

**第二阶段功能增强（待实现）：**
1. 多题库顺序查询模式 (#296)
2. 暂停答题快捷键 (#299)
3. 视频快速跳过功能 (#304)
4. 学习通考试反作弊功能 (#312)
5. 大模型自动点击验证码 (#284)

---

### 🙏 致谢

感谢以下issue的提出者提供详细的问题分析：
- #317 @InvWorld - 提供了详细的问题定位和修复方案
- #281 提供了深入的技术分析和解决方案
- #279 提供了具体的代码改进建议

---

**构建版本：** 4.15.3+
**提交日期：** 2026-07-15
**测试状态：** 待验证
