# OCS 项目优化方案

## 分析日期：2026-07-15

## 一、项目概况
OCS (Online Course Script) 是一个在线网络课程辅助工具，支持多个主流平台：
- 超星学习通 (cx)
- 知到智慧树 (zhs)
- 智慧职教 (icve/zjy)
- 雨课堂 (yuketang)
- U校园 (unipus)
- 中国大学MOOC (icourse)

## 二、主要问题分析

### 🔴 高优先级BUG修复

#### 1. [#317] 智慧树新形态课程"只跳转必学章节"功能失效
**问题描述：**
- 在 `packages/scripts/src/projects/zhs.ts` 的 `getNextJob` 函数中，未参考 `switchMode` 配置
- 导致"只跳转必学章节"模式无法正确跳过"选学资源"

**解决方案：**
修改 `getNextJob` 函数，根据 `switchMode` 配置筛选卡片：
- `job` 模式：只收集必学区域的卡片
- `all` 模式：收集所有卡片

**影响范围：** zhs.ts 1162-1187行

---

#### 2. [#281] ScriptCat环境下题库请求超时问题
**问题描述：**
- ScriptCat 基于 Service Worker，有30秒空闲自动挂起机制
- Service Worker 挂起后，`GM_xmlhttpRequest` 的回调永远不会触发
- 用户需等待完整的"搜题最大耗时"才能得到超时错误

**解决方案：**
1. 给 `request` 函数增加 `timeout` 参数
2. `GM_xmlhttpRequest` 加上 `timeout` 和 `ontimeout` 回调
3. `fetch` 使用 `AbortController` 实现超时
4. `onerror` 传递 Error 实例而非原始对象

**影响范围：** packages/core/src/core/utils/request.ts

---

#### 3. [#279] 多题库不同答案时的处理逻辑优化
**问题描述：**
- 使用多个题库时，若返回不同答案，会走相似匹配流程
- 应该优先选择"出现次数最多"的答案

**解决方案：**
在答案处理逻辑中增加投票机制，统计出现次数最多的答案

**影响范围：** packages/core/src/core/worker/question.resolver.ts

---

#### 4. [#287] 填空题编辑框插入js代码问题
**问题描述：**
填空题编辑框会把js代码插入到答案里，导致答题错误

**解决方案：**
对填空题答案进行sanitize处理，过滤script标签和危险代码

---

#### 5. [#280] 智慧树共享课自动保存问题
**问题描述：**
无论如何设置，答题后都会自动保存

**解决方案：**
检查并修复智慧树共享课的保存逻辑

---

### 🟡 功能增强

#### 6. [#296] 多题库按优先级依次查询
**功能说明：**
支持配置多个题库后，按顺序依次查询而非并发：
- 先查题库A，找到答案后停止
- A没结果再查B，以此类推
- 减少无效请求，节省题库次数

**实现方案：**
添加题库查询模式配置：并发模式 / 顺序模式

---

#### 7. [#304] 跳过视频功能
**功能说明：**
有些课程视频可以直接拖动进度条到最后跳过

**实现方案：**
检测视频是否支持拖动，如支持则提供快速跳过选项

---

#### 8. [#299] 暂停答题快捷键
**功能说明：**
为暂停答题功能添加快捷键支持

**实现方案：**
添加快捷键监听（如 Ctrl+Shift+P）

---

#### 9. [#312] 学习通考试反作弊功能
**功能说明：**
禁用切屏检测、键盘拦截、截图监控、终端锁定、调试器检测

**实现方案：**
添加可选配置项，覆盖相关检测函数

---

### 🔵 代码质量改进

#### 10. [#305] CI类型检查覆盖packages/scripts（已关闭）
这个问题已经在之前修复

#### 11. 搜题最大耗时限制优化（#260已关闭）
- 将max从60秒增加到180秒（3分钟）
- 将defaultValue从60秒增加到120秒

---

## 三、实施计划

### 第一阶段：核心BUG修复（高优先级）
1. ✅ 修复智慧树getNextJob函数
2. ✅ 修复request.ts超时问题
3. ✅ 优化多题库答案选择逻辑
4. ✅ 修复填空题编辑框问题

### 第二阶段：功能增强
5. 多题库顺序查询模式
6. 暂停答题快捷键

### 第三阶段：测试与验证
7. 单元测试
8. 集成测试
9. 用户反馈收集

---

## 四、技术债务
- 代码注释补充
- 类型定义完善
- 错误处理优化
- 日志系统改进

---

## 五、预期效果
1. 修复智慧树新课程跳转问题，提升用户体验
2. 解决ScriptCat环境下的超时问题，提高稳定性
3. 优化多题库答案选择，提高答题准确率
4. 增强代码健壮性和可维护性

---

生成时间：2026-07-15
